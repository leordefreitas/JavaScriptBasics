//  on if condicion is something that will allow us to execute one
// piece of code conditionally based on the result of some teste
// (boolean expression)
if (true) {
    console.log('1 The thing inside the parentheses nexto the if is true')
}

if (false) {
    console.log('2 The thing inside the parentheses nexto the if is true')
}