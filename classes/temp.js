/* const row = ['x', 'o', 'x'];
let xCount = 0;
let oCount = 0;
for (let i = 0; i < row.length; i++) {
  if (row[i] === 'x') xCount++;
  else if (row[i] === 'o') oCount++;
}
console.log(xCount, oCount); */

for (let i = 0; i < 3; i++) {
  const value = [];
  for(let j = o; j < 3; j++) {
    value.push(board[j][i]);
  }
}